<Window x:Class="SidePeek.App.Views.DockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:sw="clr-namespace:System.Windows;assembly=PresentationFramework"
        Title="SidePeek"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="False"
        Background="Transparent"
        WindowStartupLocation="Manual"
        Left="-32000"
        Top="-32000"
        Width="1"
        Height="1"
        Opacity="0"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        RenderOptions.ClearTypeHint="Enabled"
        TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextHintingMode="Fixed"
        FontFamily="{x:Static sw:SystemFonts.MessageFontFamily}"
        Loaded="OnLoaded">

    <Window.Resources>
        <!-- 分段式 Tab 切换按钮 -->
        <Style x:Key="TabToggle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" CornerRadius="8" Background="Transparent" Padding="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderIconButton" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="6" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="BackdropTintLayer">
        <Border.Background>
            <!-- 半透明覆盖层叠加在系统亚克力之上，保留可见的背景模糊 -->
            <SolidColorBrush Color="#FFFFFF" Opacity="0.24" />
        </Border.Background>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 头部 -->
            <DockPanel Grid.Row="0" Margin="0,0,0,14">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Margin="0,0,8,0"
                             Fill="{DynamicResource AccentFillColorDefaultBrush}" />
                    <TextBlock Text="SidePeek" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource HeaderIconButton}" Click="OnOpenSettings" ToolTip="设置">
                        <ui:SymbolIcon Symbol="Settings24" />
                    </Button>
                </StackPanel>
            </DockPanel>

            <!-- Tab 切换 -->
            <Border Grid.Row="1" Margin="0,0,0,14" CornerRadius="10" Padding="4"
                    Background="{DynamicResource ControlFillColorDefaultBrush}">
                <UniformGrid Columns="4">
                    <RadioButton x:Name="TabNotes" Style="{StaticResource TabToggle}"
                                 GroupName="Tabs" IsChecked="True" Checked="OnTabChecked">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Note24" FontSize="18" Margin="0,0,6,0" />
                            <TextBlock Text="便签" VerticalAlignment="Center" />
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="TabCommands" Style="{StaticResource TabToggle}"
                                 GroupName="Tabs" Checked="OnTabChecked">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Rocket24" FontSize="18" Margin="0,0,6,0" />
                            <TextBlock Text="命令" VerticalAlignment="Center" />
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="TabWidgets" Style="{StaticResource TabToggle}"
                                 GroupName="Tabs" Checked="OnTabChecked">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Apps24" FontSize="18" Margin="0,0,6,0" />
                            <TextBlock Text="工具" VerticalAlignment="Center" />
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="TabClipboard" Style="{StaticResource TabToggle}"
                                 GroupName="Tabs" Checked="OnTabChecked">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Clipboard24" FontSize="18" Margin="0,0,6,0" />
                            <TextBlock Text="剪切板" VerticalAlignment="Center" />
                        </StackPanel>
                    </RadioButton>
                </UniformGrid>
            </Border>

            <!-- 内容 -->
            <ContentControl x:Name="ContentHost" Grid.Row="2" />
        </Grid>
    </Border>
</Window>
