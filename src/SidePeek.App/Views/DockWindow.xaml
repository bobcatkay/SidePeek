<Window x:Class="SidePeek.App.Views.DockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="SidePeek"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="False"
        Background="Transparent"
        UseLayoutRounding="True"
        TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        TextOptions.TextFormattingMode="Display"
        FontFamily="Segoe UI"
        Loaded="OnLoaded">

    <Window.Resources>
        <!-- 分段式 Tab 切换按钮 -->
        <Style x:Key="TabToggle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" CornerRadius="8" Background="Transparent" Padding="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EdgeButton" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="6" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border>
        <Border.Background>
            <!-- 半透明白色覆盖层，叠加在系统亚克力之上，形成亮色毛玻璃 -->
            <SolidColorBrush Color="#FFFFFF" Opacity="0.4" />
        </Border.Background>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 头部 -->
            <DockPanel Grid.Row="0" Margin="0,0,0,14">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Margin="0,0,8,0"
                             Fill="{DynamicResource AccentFillColorDefaultBrush}" />
                    <TextBlock Text="SidePeek" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource EdgeButton}" Click="OnDockLeft" ToolTip="吸附到左边">
                        <ui:SymbolIcon Symbol="ArrowLeft24" />
                    </Button>
                    <Button Style="{StaticResource EdgeButton}" Click="OnDockTop" ToolTip="吸附到上边">
                        <ui:SymbolIcon Symbol="ArrowUp24" />
                    </Button>
                    <Button Style="{StaticResource EdgeButton}" Click="OnDockRight" ToolTip="吸附到右边">
                        <ui:SymbolIcon Symbol="ArrowRight24" />
                    </Button>
                    <Button Style="{StaticResource EdgeButton}" Click="OnDockBottom" ToolTip="吸附到下边">
                        <ui:SymbolIcon Symbol="ArrowDown24" />
                    </Button>
                </StackPanel>
            </DockPanel>

            <!-- Tab 切换 -->
            <Border Grid.Row="1" Margin="0,0,0,14" CornerRadius="10" Padding="4"
                    Background="{DynamicResource ControlFillColorDefaultBrush}">
                <UniformGrid Columns="3">
                    <RadioButton x:Name="TabNotes" Style="{StaticResource TabToggle}"
                                 GroupName="Tabs" IsChecked="True" Checked="OnTabChecked">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Note24" FontSize="18" Margin="0,0,6,0" />
                            <TextBlock Text="便签" VerticalAlignment="Center" />
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="TabCommands" Style="{StaticResource TabToggle}"
                                 GroupName="Tabs" Checked="OnTabChecked">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Rocket24" FontSize="18" Margin="0,0,6,0" />
                            <TextBlock Text="命令" VerticalAlignment="Center" />
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="TabWidgets" Style="{StaticResource TabToggle}"
                                 GroupName="Tabs" Checked="OnTabChecked">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Apps24" FontSize="18" Margin="0,0,6,0" />
                            <TextBlock Text="工具" VerticalAlignment="Center" />
                        </StackPanel>
                    </RadioButton>
                </UniformGrid>
            </Border>

            <!-- 内容 -->
            <ContentControl x:Name="ContentHost" Grid.Row="2" />
        </Grid>
    </Border>
</Window>
